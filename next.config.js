// /** @type {import('next').NextConfig} */
// const nextConfig = {}

// module.exports = nextConfig






/// next.config.js
module.exports = {
    // This tells Next.js to generate a static HTML export of your project
    output: 'export',
  
    // Other configurations like enabling React strict mode (optional but recommended)
    reactStrictMode: true,
  };
  
